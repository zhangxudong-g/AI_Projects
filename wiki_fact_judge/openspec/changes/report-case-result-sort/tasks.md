## 1. 准备和调研

- [x] 1.1 分析当前 `export_plan_reports_to_markdown` 函数的代码结构
- [x] 1.2 确定需要排序的两个位置（总结表格和详细结果）

## 2. 实现文件类型提取功能

- [x] 2.1 创建 `extract_file_type` 辅助函数，从 case name 提取文件扩展名
- [x] 2.2 处理大小写转换和边界情况（无扩展名、多扩展名）

## 3. 实现排序逻辑

- [x] 3.1 创建 `sort_cases_by_file_type` 函数，实现分组排序逻辑
- [x] 3.2 按文件类型分组（java、sql、py 等）
- [x] 3.3 类型间按字母排序，类型内按 case name 排序
- [x] 3.4 无扩展名的 case 排在最后

## 4. 集成到导出函数

- [x] 4.1 在"案例结果总结"表格生成前应用排序
- [x] 4.2 在"各案例详细结果"部分应用相同的排序顺序
- [x] 4.3 确保两个部分的顺序一致

## 5. 测试验证

- [x] 5.1 使用现有的 kimi plan 测试数据验证排序功能
- [x] 5.2 确认 Java 和 SQL 文件分别聚集显示
- [x] 5.3 验证排序后导出文件的正确性

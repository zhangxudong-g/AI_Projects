# Calculator 类文档

Calculator 是一个简单的计算器类，提供基本的数学运算功能。

## 功能

- `add(a, b)` - 返回两个整数的和
- `subtract(a, b)` - 返回两个整数的差
- `multiply(a, b)` - 返回两个整数的积

## 使用示例

```java
Calculator calc = new Calculator();
int result = calc.add(5, 3); // 结果为 8
```

## 注意事项

- 所有方法都接受整数参数
- 所有方法都返回整数值
- 没有处理溢出情况